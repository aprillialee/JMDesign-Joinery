import styled from "styled-components";

function OurStoryPage() {
  return <OurStoryStyled>Hello</OurStoryStyled>;
}

const OurStoryStyled = styled.div`
  width: 100vw;
  height: 100%;
  background: #0c072c;
`;

export default OurStoryPage;
